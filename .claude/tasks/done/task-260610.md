# 마스터 인덱스 — InfluencerSeeder v3 (prd-260610-3) + 리팩터 + flow-builder

> 생성: 2026-06-10 · 완료: 2026-06-10
> 상태: ✅ **전체 완료** — 모든 push/refactor/flow-builder 실행됨. 최종 `pytest tests/ -v` → **242 passed**. 모든 src/*.py ≤ 500줄.

## 실행 결과 (실행 순서대로)

| 작업 | 범위 | 결과 | 결과보고서 |
|------|------|------|-----------|
| push1 | storage 5분리 + dedup + state.json | ✅ 83→ | `done/result-prd-260610-3-push1.md` |
| push2 | scraper stealth/resume/flow 최적화 | ✅ 124 | `done/result-prd-260610-3-push2.md` |
| refactor-1 | scraper.py 917→399 + Flow/Step 추상화 | ✅ 136 | `done/result-refactor-1-scraper.md` |
| refactor-2 | storage.py 508→117 (관심사 분리) | ✅ 140 | `done/result-refactor-2-storage.md` |
| push4 | 메인윈도우 트레이/이어하기/차단/로그/진행표시 | ✅ 185 | `done/result-prd-260610-3-push4.md` |
| push3 (병합) | 설정 기본 탭(웹/시간텀/플로우/타겟/제외) | ✅ 212 | `done/result-prd-260610-3-push3.md` |
| flow-builder-4 | ConfigurableFlow + 버튼매핑 카드 + 플로우 빌더 + 폴더 공유 | ✅ 242 | `done/result-260610-4-flow-builder.md` |
| refactor-3 | settings_view.py 990→143 (믹스인 분리) | ✅ 242 | `done/result-refactor-3-settings.md` |

## 의사결정 기록
- **설정 UI 병합:** push3 는 기본 탭만, 버튼매핑/플로우/import-export 는 flow-builder 가 카드형/폴더형으로(중복 제거). 순서 push4 → 설정. (사용자 확정 2026-06-10)
- **500줄 규칙:** 사용자 상시 지시 — scraper(917)/storage(508)/settings_view(990) 전부 리팩터링 완료. 현재 최대 파일 < 500.
- **monkeypatch 계약 보존:** `core/scraper.py`·`core/storage.py` 는 모듈(파사드) 유지, 역량 메서드·DATA_DIR·patch 대상 이름 보존.

## 최종 구조 요약
- `core/`: scraper.py(399) + scraper_driver/parsing + flows/(base/context/steps/hashtag/configurable + registry) ; storage.py(파사드) + storage_{defaults,config,selectors,results,state,flowsteps,share} ; run_logger, assets.
- `ui/`: main_window(트레이/이어하기/차단/로그) · settings_view(코디네이터) + settings/*_tab 믹스인 · panels · widgets.
- 신규 플로우 추가 = `Flow` 서브클래스 + `register()` 또는 `flow_steps.csv` 편집(ConfigurableFlow).

## 비고
- git 로컬 저장소 초기화됨(원격 없음 → push 없음). 모든 하위 작업 커밋 단위 분리.
- 무관 파일 `src/run_mac.command`/`run_windows.bat` 는 본 작업 범위 밖(미수정).
- 남은 수동검증: 실제 GUI(`python main.py`) 에서 버튼매핑 카드 렌더·플로우 표·폴더 다이얼로그 — 헤드리스라 자동검증 불가분.
